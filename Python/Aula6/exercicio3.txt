def file_info(texto):

    texto = open (texto,"r",encoding= "utf-8") #Tirar linha
    palavras_separadas = []#Tirar linha
    quant_palavras = 0#Tirar linha
    quant_letras = 0 #Tirar linha

    for linha in texto.readlines() :#Tirar linha
        quant_letras += len(linha)#Tirar linha
        linha_separada  = linha.split(" ")
        palavras_separadas.append(linha_separada) #Tirar linha

    for i in range(len(palavras_separadas)) :#Tirar linha
        for palavras in palavras_separadas[i] :
            quant_palavras += 1 #Tirar linha#Tirar linha
    return (len(palavras_separadas),quant_palavras,quant_letras)#Tirar linha